plugins {
    id 'java'
    id 'maven-publish'
}

group = 'org.gradle.sample'
version = '1.0'

// tag::publish-optional-artifact[]
task sourcesJar(type: Jar) {
    archiveClassifier = 'sources'
    from sourceSets.main.allJava
}

task javadocJar(type: Jar) {
    archiveClassifier = 'javadoc'
    from javadoc.destinationDir
    enabled = false
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java

            artifact sourcesJar
            artifact(javadocJar) {
                optional = true
            }
        }
    }
}
// end::publish-optional-artifact[]

publishing {
    repositories {
        maven {
            url = "$buildDir/repo"
        }
    }
}

